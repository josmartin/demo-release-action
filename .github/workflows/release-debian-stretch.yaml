name: "release-debian-stretch"

on:
  workflow_dispatch:

jobs:
  release-debian-stretch:
    uses: josmartin/demo-release-action/.github/workflows/build-and-release.yaml
      with: 
        dist-base: debian
        dist-tag: stretch
